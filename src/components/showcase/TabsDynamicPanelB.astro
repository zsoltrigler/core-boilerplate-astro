<p class="text-sm text-(--color-text-muted)">
  Panel B — also built from a data array via <code class="rounded bg-(--color-bg-surface) px-1"
    >.map()</code
  >, which named slots can't do (see CONTRIBUTING.md).
</p>
